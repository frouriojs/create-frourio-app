{
  "name": "frourio-app-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run generate && <% if (orm === 'prisma') { %>npm run migrate:dev && <% } %>run-p dev:*",
    "dev:server": "node-dev --notify=false --respawn index.js",
    "dev:build": "node ./scripts/build.dev.js",
    "dev:frourio": "frourio<% if (server === 'express') { %>-express<% } %> --watch<% if (orm === 'prisma') { %>",
    "dev:prisma": "prisma generate --watch<% } %>",
    "build": "npm run generate && node ./scripts/build.prod.js",
    "generate": "run-p generate:*",
    "generate:frourio": "frourio<% if (server === 'express') { %>-express<% } %><% if (orm === 'prisma') { %>",
    "generate:prisma": "prisma generate<% } %><% if (orm === 'prisma') { %>",
    "migrate:dev": "prisma migrate dev",
    "migrate:dev:createonly": "prisma migrate dev --create-only",
    "migrate:reset": "prisma migrate reset<% } %><% if (orm === 'prisma'){ %>",
    "seed": "prisma db seed --preview-feature",
    "ts-node": "ts-node --compiler-options \"{\\\"module\\\":\\\"commonjs\\\"}\"<% } %>",
    "start": "cross-env NODE_ENV=production node index.js"
  }
}
