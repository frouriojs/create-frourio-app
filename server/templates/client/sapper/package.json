{
  "name": "TODO",
  "description": "TODO",
  "version": "0.0.1",
  "scripts": {
    "dev": "<% if (orm === 'prisma') { %>npm run migrate:up && <% } %>run-p dev:*",
    "dev:client": "sapper dev -p <%= clientPort %>",
    "dev:server": "npm run dev --prefix server",
    "dev:aspida": "aspida --watch",
    "build": "run-p build:client build:server",
    "build:client": "aspida && sapper <%= building === 'basic' ? 'build' : 'export' %><%= bundler === 'rollup' ? '--legacy' : '' %>",
    "build:server": "npm run build --prefix server",
    "build:types": "aspida && npm run build:frourio --prefix server",
    "lint": "cd server && eslint --ext .ts,.js .",
    "lint:fix": "npm run lint -- --fix<% if (orm === 'prisma') { %>",
    "migrate": "npm run migrate:save && npm run migrate:up",
    "migrate:save": "npm run migrate:save --prefix server",
    "migrate:up": "npm run migrate:up --prefix server",
    "migrate:down": "npm run migrate:down --prefix server<% } else if (orm === 'typeorm') { %>",
    "migration:generate": "npm run migration:generate --prefix server",
    "migration:run": "npm run migration:run --prefix server",
    "migration:revert": "npm run migration:revert --prefix server<% } %>",
    "start": "run-p start:*",
    "start:client": "<%= building === 'basic' ? 'node __sapper__/build' : 'serve __sapper__/export' %>",
    "start:server": "npm start --prefix server",
    "validate": "svelte-check --ignore src/node_modules/@sapper<% if (testing === 'jest') { %>",
    "test": "npm run build:types && jest<% } %>",
    "typecheck": "npm run build:types && npm run validate && tsc --noEmit -p server"
  },
  "dependencies": {
    "<% if (aspida === 'axios') { %>@aspida/axios": "^0.12.2",
    "axios": "^0.21.0",
    "<% } else { %>@aspida/fetch": "^0.10.2",
    "<% } %>compression": "^1.7.1",
    "class-validator": "^0.12.2",
    "polka": "next",
    "sirv": "^1.0.0"
  },
  "devDependencies": {
    "<% if (testing === 'jest') { %>@types/jest": "^26.0.15",
    "eslint-plugin-jest": "^24.1.3",
    "jest": "^26.6.3<% } %>",
    "@typescript-eslint/eslint-plugin": "^4.8.2",
    "@typescript-eslint/parser": "^4.8.2",
    "dotenv": "^8.2.0",
    "eslint": "^7.14.0",
    "eslint-config-prettier": "^6.15.0",
    "eslint-config-standard": "^16.0.2",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^3.1.4",
    "eslint-plugin-promise": "^4.2.1",
    "npm-run-all": "^4.1.5",
    "sapper": "^0.28.0",
    "svelte": "^3.17.3<% if (bundler === 'webpack') { %>",
    "file-loader": "^6.0.0",
    "svelte-loader": "^2.9.0",
    "webpack": "^4.7.0",
    "webpack-modules": "^1.0.0",
    "ts-loader": "^8.0.4",
    "tsconfig-paths-webpack-plugin": "^3.3.0<% } else { %>",
    "@babel/core": "^7.0.0",
    "@babel/plugin-syntax-dynamic-import": "^7.0.0",
    "@babel/plugin-transform-runtime": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "@babel/runtime": "^7.0.0",
    "@rollup/plugin-babel": "^5.0.0",
    "@rollup/plugin-commonjs": "^14.0.0",
    "@rollup/plugin-node-resolve": "^8.0.0",
    "@rollup/plugin-replace": "^2.2.0",
    "@rollup/plugin-url": "^5.0.0",
    "rollup": "^2.3.4",
    "rollup-plugin-svelte": "^6.0.0",
    "rollup-plugin-terser": "^7.0.0",
    "@rollup/plugin-typescript": "^6.0.0<% } %>",
    "@tsconfig/svelte": "^1.0.10",
    "@types/compression": "^1.7.0",
    "@types/node": "^14.11.1",
    "prettier": "^2.2.1<% if (building === 'static') { %>",
    "serve": "^11.3.2<% } %>",
    "svelte-check": "^1.0.46",
    "svelte-preprocess": "^4.3.0<% if (testing === 'jest') { %>",
    "ts-jest": "^26.4.4<% } %>",
    "tslib": "^2.0.1",
    "typescript": "^4.1.2"
  }
}