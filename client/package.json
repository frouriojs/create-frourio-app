{
  "scripts": {
    "build": "run-p build:next build:lib",
    "build:next": "aspida && next build && next export",
    "build:lib": "webpack --config webpack.config.lib.js",
    "typecheck": "tsc --pretty --noEmit"
  }
}
